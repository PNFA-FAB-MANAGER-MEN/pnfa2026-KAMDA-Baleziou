# JOURNAL - du jeudi 03-09-2026 (rédigé par Baleziou KAMDA)

## Fait aujourd'hui

Aujourd'hui, nous avons entamé l'apprentissage de la programmation en Python dans le cadre du cours pour les FabManagers.

- **Installation de l'environnement de développement :** Téléchargement et installation du logiciel **Thonny IDE**.
- **Cours Python FabManagers :** Découverte des notions de base du langage Python et de sa syntaxe.
- **Exercice pratique :** Écriture et exécution d'un script interactif qui demande à l'utilisateur de saisir son nom et son âge, puis affiche un résumé personnalisé des informations saisies.

## Appris

- **Types de données :** Définition des chaînes de caractères (*string*) pour manipuler le texte et les lettres.
- **Syntaxe de base en Python :** Utilisation des fonctions `input()` pour récupérer une saisie utilisateur et `print()` pour afficher le résultat.
- **Variables et concaténation :** Stockage des réponses dans des variables et assemblage des textes pour générer un message de synthèse.

## Raté, puis corrigé

- **Erreur de type lors de la saisie (Python) :** Prise en compte de l'âge comme une chaîne de caractères au lieu d'un nombre entier pour d'éventuels calculs.
  - *Correction :* Conversion du résultat de la fonction `input()` avec `int()` lorsque l'âge doit être manipulé numériquement.

## Décider

- Practiquer les scripts Python de base sur Thonny pour se familiariser avec la syntaxe avant d'aborder MicroPython sur l'ESP32-S3.

## À faire demain

- Poursuivre les exercices de programmation Python et aborder les structures conditionnelles (`if`, `else`).